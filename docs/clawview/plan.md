### A. 计划摘要
本 v2 计划继续以“可观测性与风险发现”为唯一目标，按 **M1 指标口径冻结 -> M2 数据链路落地 -> M3 Display-Only 看板成型 -> M4 试运行与验收** 推进。  
相对 v1，v2 补齐 6 个阻塞项：  
1. 每个里程碑与阶段任务都明确了 `Owner + 截止时间`。  
2. 增加首发最小上线子集 **P0-Core（12 项）**，避免首发过重。  
3. 增加 `endpoint_group` 分组字典草案与治理规则。  
4. 增加重启分类优先级规则（Planned/Unexpected/Unknown）。  
5. 增加阈值冷启动校准机制（试运行后自动建议，人工确认生效）。  
6. 为验证门槛补齐量化阈值（覆盖率、延迟、准确性）。

---

### B. 范围与非范围（Scope / Non-Scope）
**Scope**
1. 首发上线按 **P0-Core（12 项）** 验收，保障“10 秒判断健康、30 秒定位风险”可用。
2. P0 全量字段（34 项）继续完成口径冻结与数据映射，但不作为首发阻塞项。
3. 双窗口统一口径：Rolling 24h + Asia/Tokyo 当日。
4. 首页调用次数固定为双口径展示：Rolling 24h（主）+ Tokyo 今日（辅）；周/月仅在 API 详情页展示。
5. 看板刷新延迟目标：P95 <= 15 分钟，P99 <= 20 分钟。
6. 风险发现聚焦三类：触发风暴、限速（429）、异常重启。
7. 看板信息架构固定为：顶部 KPI、中部趋势、侧边 Skill 模块、底部明细。
8. 首页只放轻量摘要，不放全量明细；明细统一下钻到子模块页（Skill/Cron/API/Error）。
9. Dashboard 首屏突出要素固定为 4 类：健康状态、重启风险、限速风险、错误风险。
10. 首页模块固定清单：健康总览（1 行）+ 趋势图（2 张）+ Skill/Cron/API 三个摘要模块。
11. Skill 模块首页仅展示摘要 + TopN（默认 5，可配置 6），全量 80~100+ Skill 通过详情页/抽屉下钻查看。
12. 原始快照 48h + 每日压缩快照长期保留。
13. 告警只做状态与解释，不触发控制动作。

**首发上线子集（P0-Core，12 项）**
| 序号 | 指标 | 说明 |
|---|---|---|
| 1 | service_uptime_ratio_24h | 服务 24h 可用率 |
| 2 | service_status_now | 当前健康状态（正常/降级/异常） |
| 3 | trigger_total_24h | 任务触发总量 |
| 4 | trigger_storm_task_top5_5m | 5 分钟触发风暴 Top5 |
| 5 | api_call_total_24h | API 调用总量 |
| 6 | api_error_rate_24h | API 错误率 |
| 7 | api_429_ratio_24h | 限速比例（429） |
| 8 | endpoint_group_top5_calls_24h | endpoint_group 调用量 Top5 |
| 9 | error_fingerprint_top10_24h | 高频错误 Top10 |
| 10 | restart_unexpected_count_24h | 异常重启次数 |
| 11 | data_freshness_delay_min | 数据新鲜度延迟（分钟） |
| 12 | p0_core_coverage_ratio | P0-Core 数据覆盖率 |

**Skill 模块分期（与 PRD 对齐）**
- Phase 1（首发）：Skill 总数、模块简介、24h 调用总次数、TopN（默认 5，可配置 6）
- Phase 2（增强）：全量 Skill 列表（分页/搜索）、单 Skill 趋势与错误率、调用明细

**首页 TopN 展示条数规则（响应式）**
- Skill TopN：手机 5 / Web 6
- Cron 风险 TopN：手机 3 / Web 5
- API 分组 TopN：手机 3 / Web 5

**Non-Scope**
1. 不做任何反向控制（暂停任务、重启服务、改配置）。
2. 不做复杂分布式链路追踪与多租户权限系统。
3. v1 首发不引入 P95/P99 之外的新高级性能指标作为阻塞项。
4. 不保留旧口径并行展示（避免双标导致误判）。
5. 不把 P0-Full（剩余 22 项）延期风险转嫁给首发验收。

---

### C. 里程碑（M1~M4）
| 里程碑 | Primary Owner | 协同角色 | 截止时间 | 目标 | 产出物 | 通过门槛 |
|---|---|---|---|---|---|---|
| M1 口径冻结 | DA（数据负责人） | PO、SRE | 2026-03-06 | 固化 SoT 与边界 | v2 指标字典、阈值表、Display-Only 边界说明、重启判定规则、endpoint_group 字典草案 | P0 34 项定义完成；P0-Core 12 项冻结；关键规则评审通过 |
| M2 链路就绪 | BE（后端负责人） | DA、QA | 2026-03-20 | 采集/聚合/存储链路可用 | 数据链路设计包、质量校验清单、缺口清单 | P0-Core 覆盖率 >= 98%；质量状态可判定 |
| M3 看板联调 | FE（前端负责人） | BE、OPS | 2026-04-03 | 首版看板可巡检可定位 | KPI+趋势+明细联动页、告警状态映射、质量态上屏 | 达成“10 秒健康判断、30 秒定位两类风险”演练 |
| M4 试运行验收 | QA（测试负责人） | SRE、PO、DA | 2026-04-17 | 连续试运行并收敛阈值 | 试运行报告、阈值校准建议单、验收记录、发布清单 | 延迟/准确性/可用性量化门槛全部通过 |

---

### D. 分阶段任务清单（每阶段 5-10 项）
**阶段 1（M1：口径与治理冻结）**
| 任务 | Owner | 截止时间 | 交付物 |
|---|---|---|---|
| 建立唯一《ClawView v1 指标字典》SoT | DA | 2026-02-27 | 字典初版 |
| 完成 P0 34 项定义（公式/窗口/单位/Owner） | DA | 2026-02-28 | 字段定义表 |
| 冻结 P0-Core 12 项首发清单 | PO | 2026-02-28 | 首发范围确认单 |
| 冻结首页调用次数窗口口径（24h 主 + Tokyo 今日辅） | DA | 2026-03-01 | 首页口径确认单 |
| 输出重启分类优先级规则 | SRE | 2026-03-01 | 判定规则文档 |
| 输出 endpoint_group 字典 v0.1 | BE | 2026-03-02 | 分组字典草案 |
| 固化错误指纹归一化与 TopN 规则 | BE | 2026-03-03 | 错误口径说明 |
| 固化数据质量状态标准 | QA | 2026-03-04 | 质量判定标准 |
| 完成 M1 评审并冻结基线 | PO | 2026-03-06 | M1 评审记录 |

**阶段 2（M2：数据链路与质量门禁）**
| 任务 | Owner | 截止时间 | 交付物 |
|---|---|---|---|
| 数据源盘点并映射到 P0/P0-Core | BE | 2026-03-09 | 数据源映射表 |
| 统一事件模型（时间戳/来源/窗口/质量标签） | BE | 2026-03-10 | 事件模型说明 |
| 双窗口聚合口径对齐 | DA | 2026-03-11 | 聚合口径表 |
| 派生指标计算路径确定 | DA | 2026-03-12 | 派生指标清单 |
| 规划存储分层与保留周期 | BE | 2026-03-13 | 存储策略 |
| 建立四类质量检查（完整性/延迟/重复/异常值） | QA | 2026-03-16 | 质量门禁清单 |
| 缺失字段展示策略冻结（显示缺失，不伪造） | FE | 2026-03-17 | 展示兜底规范 |
| 输出 M2 验证报告与遗留风险 | QA | 2026-03-20 | M2 验证报告 |

**阶段 3（M3：展示与告警联调）**
| 任务 | Owner | 截止时间 | 交付物 |
|---|---|---|---|
| IA 冻结：KPI/趋势/明细结构 | FE | 2026-03-24 | 页面结构评审稿 |
| 首页信息密度规则冻结（仅摘要，不放全量明细） | FE | 2026-03-25 | 首页信息密度规范 |
| Dashboard 首屏突出要素冻结（健康/重启/限速/错误） | FE | 2026-03-25 | 首屏要素清单 |
| 首页固定模块清单冻结（1 行总览 + 2 张趋势 + 3 摘要模块） | FE | 2026-03-25 | 首页模块清单 |
| Skill 模块首发摘要实现（总数/简介/24h 调用/TopN） | FE | 2026-03-25 | Skill 模块首发验收稿 |
| Skill 模块下钻方案（详情页/抽屉）对齐 PRD | FE | 2026-03-26 | 下钻交互说明 |
| TopN 响应式条数规则落地（手机/Web） | FE | 2026-03-26 | 响应式规则验收记录 |
| 所有指标展示窗口标签与更新时间 | FE | 2026-03-26 | UI 规范确认 |
| 告警状态映射与解释文案冻结 | PO | 2026-03-26 | 告警文案表 |
| 数据质量状态上屏（完整/缺失/延迟） | FE | 2026-03-27 | 联调截图与记录 |
| Top5 触发风暴、Top10 错误、API 明细联动 | FE | 2026-03-30 | 联动验收记录 |
| Display-Only 合规检查 | BE | 2026-03-31 | 合规检查单 |
| 运营走查巡检路径与定位路径 | OPS | 2026-04-01 | 走查反馈清单 |
| 完成 M3 出口评审 | PO | 2026-04-03 | M3 出口记录 |

**阶段 4（M4：试运行与上线决策）**
| 任务 | Owner | 截止时间 | 交付物 |
|---|---|---|---|
| 连续试运行 7 天并采样 | SRE | 2026-04-12 | 试运行数据集 |
| 执行三类演练（触发风暴/限速暴涨/异常重启） | QA | 2026-04-13 | 演练记录 |
| 按量化阈值验证 AC | QA | 2026-04-14 | 验收打分表 |
| 统计误报率/漏报率与分类准确性 | DA | 2026-04-15 | 精度报告 |
| 生成冷启动阈值自动建议单 | DA | 2026-04-15 | 建议单 v1 |
| 召开阈值评审并完成一次微调 | PO | 2026-04-16 | 阈值变更记录 |
| 冻结发布清单与运行手册 | PO | 2026-04-16 | 发布包清单 |
| Go/No-Go 评审结论 | PO | 2026-04-17 | 上线决议 |

---

### E. 指标字典冻结策略（P0/P1 与口径）
1. **唯一 SoT**：仅一份《ClawView v1 指标字典》生效；图表、告警、周报统一引用。
2. **发布分层**：  
   - 首发阻塞层：P0-Core 12 项。  
   - 首发非阻塞层：P0-Full 剩余 22 项，进入 v1.1 补齐。
3. **P0 冻结规则**：34 项都必须完成定义与口径，但首发验收以 P0-Core 为准。
4. **P1 管理规则**：只登记不阻塞上线，且不得改写 P0 公式。
5. **口径总则**：统一时区、统一窗口、统一计数边界、统一异常分类。
6. **版本策略**：字典版本化；任何变更必须附影响评估、生效时间、回滚条件。
7. **变更门禁**：先改字典，再改展示与告警；禁止先实现后补文档。
8. **API 限速统一口径**：HTTP 429 与各平台等价限流错误统一计入“限速次数（429）”，并用于“限速比例（429）”计算。

**endpoint_group 分组字典草案（v0.1）**
| group_key | 典型匹配范围（示意） | 备注 |
|---|---|---|
| auth | `/auth/*` `/login` `/token/*` | 鉴权相关 |
| account | `/users/*` `/profile/*` | 账号资料 |
| contacts | `/contacts/*` | 联系人 |
| conversations | `/conversations/*` `/threads/*` | 会话 |
| message_send | `/messages/send*` | 发送路径 |
| message_receive | `/messages/inbound*` `/messages/pull*` | 接收路径 |
| media | `/media/*` `/files/*` | 媒体与文件 |
| webhooks | `/webhooks/*` | 回调入口 |
| scheduler | `/jobs/*` `/scheduler/*` | 定时任务相关 |
| integrations | `/integrations/*` `/provider/*` | 第三方适配 |
| admin_config | `/admin/*` `/config/*` | 管理配置 |
| health_metrics | `/health` `/ready` `/metrics` | 健康与观测 |
| others | 未匹配项 | 收敛桶，不可拆成原始 URL |

**endpoint_group 治理规则**
1. 仅允许使用字典中的 group_key；展示层禁止原始 URL 维度。
2. 匹配优先级：`精确路由 > 最长前缀 > 关键词 > others`。
3. `others` 占比连续 2 天 > 5%，必须发起字典增补评审。
4. 字典新增/调整按周批处理，避免日常抖动导致维度爆炸。

**重启分类判定优先级（Planned/Unexpected/Unknown）**
| 判定顺位 | 分类 | 判定条件（满足即命中） |
|---|---|---|
| 1 | Unexpected | 存在高置信异常证据：崩溃、OOM、panic、非 0 退出、健康检查失败拉起、短时间连续重启 |
| 2 | Planned | 无 Unexpected 证据，且命中维护窗口/发布工单/人工批准重启，且停启序列符合预期 |
| 3 | Unknown | 证据不足或冲突无法消解 |

**冲突处理**
1. 同时命中 Planned 与 Unexpected 时，**Unexpected 优先**，并打上 `maintenance_overlap=true` 标签。
2. 证据源可信度优先级：运行时事件/编排器原因 > 发布系统/维护工单 > 人工备注。
3. Unknown 占比目标 <= 5%；连续 3 天 > 10% 视为 P1 问题并要求补证据链。

---

### F. 数据链路计划（采集/聚合/存储/展示）
| 链路层 | 计划内容 | Owner | 截止时间 | 约束 |
|---|---|---|---|---|
| 采集 | 采集服务状态、任务触发、API 调用、错误、规模快照 | BE | 2026-03-10 | 只采观测数据，不采控制指令 |
| 标准化 | 应用 endpoint_group 映射、重启分类规则、错误指纹归一化 | DA | 2026-03-12 | 全部规则必须来自 SoT 字典 |
| 聚合 | 统一双窗口聚合，产出 KPI/趋势/TopN/告警输入 | DA | 2026-03-14 | 禁止跨窗口混算 |
| 存储 | 原始快照 48h，日压缩快照长期保留，保留质量标签 | BE | 2026-03-16 | 原始层与汇总层隔离 |
| 展示服务 | 输出带窗口口径、更新时间、质量状态的数据视图 | BE | 2026-03-19 | 缺失字段可见，不伪造 |
| 前端展示 | KPI/趋势/明细三层展示，质量态上屏 | FE | 2026-04-03 | Display-Only，无写入口 |

---

### G. 告警与阈值治理策略
1. v1 初始阈值以 PRD 已确认值为底线，不低于 PRD 下限。
2. 告警仅两级：黄灯（关注）/红灯（处置）。
3. 告警窗口与展示窗口一致，禁止跨窗口判断。
4. 数据质量前置门：数据缺失或延迟时，告警标注“可信度降低”。

**核心风险初始阈值（首发）**
| 风险 | 黄灯 | 红灯 | 窗口 | Owner |
|---|---|---|---|---|
| 触发风暴 | 同任务 5 分钟触发次数 >= 3 倍基线且 >= 20 | >= 5 倍基线且 >= 40 | 5m | SRE |
| 限速（429） | 限速比例（429） >= 3% | 限速比例（429） >= 8% | 15m | SRE |
| 异常重启 | Unexpected 重启 >= 1 次 | Unexpected 重启 >= 2 次或 10 分钟内连续 2 次 | 24h/10m | SRE |

**阈值冷启动校准机制（试运行后自动建议）**
1. 试运行采样窗口：至少 7 天连续样本。
2. 生效前提：单指标有效样本量 >= 1000 或覆盖完整 7 天窗口。
3. 自动建议规则：  
   - 比例类指标：`yellow_candidate = max(PRD_yellow, P95_stable)`，`red_candidate = max(PRD_red, P99_stable)`。  
   - 计数类指标：`yellow_candidate = max(PRD_yellow, median + 3*MAD)`，`red_candidate = max(PRD_red, median + 5*MAD)`。
4. 自动建议不自动生效，必须经过 `DA + SRE + PO` 评审确认。
5. 采纳条件：预估误报率下降 >= 20%，且召回率下降不超过 5%。
6. 调整频率：每周最多 1 次，保留前后值、证据、影响面、生效时间。

---

### H. 验证计划（如何验证计划执行质量）
1. **过程质量**：每个里程碑必须具备产出物、Owner、截止时间、评审记录。
2. **口径质量**：抽查关键指标，确保“字典定义=聚合结果=展示文案”一致。
3. **数据质量**：完整性、延迟、重复、异常值四类检查可复现。
4. **业务有效性**：按 AC 场景验收（10 秒健康判断、30 秒定位风险）。
5. **边界合规性**：逐条核对 Display-Only 清单，确认无控制能力泄漏。
6. **试运行质量**：跟踪告警准确性、看板可用性、阈值建议效果。
7. **复盘质量**：形成“问题-原因-改进”闭环并回灌下一轮批注。

**量化验收门槛（新增）**
| 维度 | 指标 | 验收阈值 | 阻塞级别 |
|---|---|---|---|
| 覆盖率 | P0-Core 指标覆盖率 | >= 98% | P0（阻塞） |
| 覆盖率 | endpoint_group 映射覆盖率 | >= 97%，且 `others` <= 3% | P0（阻塞） |
| 覆盖率 | 重启分类可判定率（非 Unknown） | >= 95% | P1 |
| 延迟 | 数据新鲜度延迟 P95 | <= 15 分钟 | P0（阻塞） |
| 延迟 | 数据新鲜度延迟 P99 | <= 20 分钟 | P1 |
| 准确性 | 告警 Precision（3 类风险汇总） | >= 85% | P0（阻塞） |
| 准确性 | 告警 Recall（3 类风险汇总） | >= 90% | P0（阻塞） |
| 准确性 | 重启分类准确率（抽样复核） | >= 90% | P1 |
| 一致性 | 口径一致性抽检通过率 | 100% | P0（阻塞） |
| 可用性 | Skill 模块首页 TopN 可读性（默认 5）+ 下钻可达性 | 100% 场景通过 | P1 |
| 可用性 | 首页信息密度合规（仅摘要，无全量明细） | 100% 场景通过 | P1 |
| 可用性 | 响应式 TopN 条数规则（手机/Web） | 100% 场景通过 | P1 |
| 合规性 | Display-Only 合规项通过率 | 100% | P0（阻塞） |

---

### I. 风险与 Plan B 触发条件
| 风险 | 触发条件 | Plan B |
|---|---|---|
| 首发范围失控 | P0-Full 被错误纳入首发阻塞，影响节奏 | 强制回到 P0-Core 12 项验收，剩余项转 v1.1 |
| 指标口径分歧 | 同一指标不同页面数值不一致 | 立即冻结发布，以 SoT 回溯并统一重算 |
| 数据覆盖不足 | P0-Core 覆盖率连续 2 天 < 98% | 缩减到三类风险核心视图，优先修复缺失数据源 |
| 延迟超标 | P95 延迟连续 1 天 > 15 分钟 | 临时降级刷新频率并高亮延迟标识，优先修复瓶颈 |
| endpoint 维度爆炸 | `others` 连续 2 天 > 5% 或 group 数失控 | 冻结新增维度，仅允许字典白名单 + others |
| 重启分类不稳定 | Unknown 连续 3 天 > 10% | 启动证据链补全专项，临时将该类风险降权展示 |
| 告警噪音过高 | Precision < 85% 且影响值班使用 | 暂停阈值扩展，只做证据驱动微调 |
| 冷启动校准失真 | 自动建议导致 Recall 下滑 > 5% | 回滚至 PRD 阈值基线，重新采样一周再评审 |
| 进度失控 | 连续两个里程碑延期且阻塞未清 | 启动范围重切，保住 P0-Core 上线目标 |

---

### J. 批注循环协议（v1->v2->v3）
1. **版本定义**：v1=初稿基线；v2=处理阻塞批注后版本；v3=上线前冻结候选版。
2. **批注输入格式**：`章节`、`问题级别(P0/P1/P2)`、`现象`、`建议`、`是否阻塞上线`。
3. **处理时限**：每轮批注 24 小时内回收并出“采纳/驳回/待定”决议表。
4. **升级规则**：存在任一 P0 阻塞不得升版；P1/P2 可进后续迭代。
5. **变更记录**：每次升版必须附变更摘要（新增/删除/修改/原因/影响里程碑）。
6. **轮次节奏**：建议最多 3 轮收敛；超过 3 轮仍有 P0 分歧则升级专项评审。
7. **冻结条件**：v3 要求“无 P0 阻塞、里程碑出口清晰、验证方案可执行”。
8. **冻结后规则**：新需求不直接改 v3，统一进入 v1.1 需求池，避免执行期 scope creep。
9. **责任归口**：P0 批注默认由对应章节 Owner 在 48 小时内给出闭环证据。

---

### K. v1->v2 变更摘要（批注处理表）
| 批注 | 级别 | 处理结果 | 落地章节 | 状态 |
|---|---|---|---|---|
| 缺少每个里程碑与阶段任务的 Owner 和截止时间 | P0 | 为 M1~M4 增加 Owner/协同/截止时间；为四阶段任务全部补齐 Owner 与日期 | C、D | 已处理 |
| 缺少最小上线子集 P0-Core（10~12 指标） | P0 | 新增 P0-Core 12 项首发清单，并明确首发阻塞层与 v1.1 补齐层 | B、E | 已处理 |
| 缺少 endpoint_group 分组字典草案 | P0 | 新增 endpoint_group v0.1 字典（13 组含 others）与匹配优先级、治理规则 | E | 已处理 |
| 缺少重启分类判定优先级规则 | P0 | 新增 Planned/Unexpected/Unknown 判定顺位、冲突处理、证据可信度优先级 | E | 已处理 |
| 缺少阈值冷启动校准机制 | P1 | 新增 7 天试运行后自动建议机制、采纳条件、评审责任与节奏 | G、D（M4） | 已处理 |
| 验证门槛缺少量化阈值 | P1 | 新增覆盖率、延迟、准确性、一致性、合规性量化验收表并标注阻塞级别 | H | 已处理 |
| Skill 作用域可能外溢（config.json 非硬门禁） | P1 | 已在 `skills/code-agent-sop/SKILL.md` 增加 frontmatter 与作用域边界说明；并新增 v1.1 follow-up：评估并实现 loader 对 `skills/**/config.json` 的硬门禁支持 | E、J | 部分处理（跟踪中） |
| Dashboard/Skill 模块定义不够具体（TopN 与下钻不清晰） | P1 | 增加看板模块结构约束（KPI/趋势/侧边 Skill/明细），新增 Skill 模块首发摘要与下钻任务，并补充可用性验收项（TopN 可读+下钻可达） | B、D、H | 已处理 |
| 首页信息过载（移动端不友好） | P1 | 强化首页与子模块边界：首页仅摘要，详情下钻；新增响应式 TopN 条数规则与首页信息密度验收项 | B、D、H | 已处理 |
| Dashboard 需要只突出关键信息，其余放侧边栏/子模块 | P1 | 增加“首屏突出要素 4 类”规则，并在计划中补充对应冻结任务；首页与子模块边界进一步收紧 | B、D、7.1 | 已处理 |
| 文档结构不清（首页与侧边栏内容混在一起） | P1 | 重写 PRD IA 章节为“首页模块清单 + 侧边栏详情清单 + 边界规则”，并在 plan 增加首页固定模块清单任务 | B、D、PRD-7 | 已处理 |
| 首页调用次数窗口不明确（周/日/月混淆） | P1 | 明确首页固定双口径：Rolling 24h（主）+ Tokyo 今日（辅）；周/月仅在 API 详情页展示，并补充口径冻结任务 | B、D、PRD-7 | 已处理 |
